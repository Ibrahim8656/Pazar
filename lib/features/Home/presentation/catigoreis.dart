import 'package:flutter/material.dart';

class CatigoreisScreen extends StatelessWidget {
  const CatigoreisScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("catigories")),
    );
  }
}